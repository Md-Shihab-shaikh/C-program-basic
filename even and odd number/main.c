#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a;
   scanf(" %d", &a);
   if(a%2==0)
    printf("it is a even number",a);

   else
    printf(" it is a odd number",a);

return 0;
}
